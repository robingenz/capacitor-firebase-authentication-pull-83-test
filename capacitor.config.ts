import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'test-capacitor-firebase-authentication-pull-83',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
